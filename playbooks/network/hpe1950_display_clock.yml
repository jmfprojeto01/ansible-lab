---
- name: Teste SSH HPE 1950
  hosts: all
  gather_facts: false

  tasks:
    - name: Executar display clock
      ansible.netcommon.cli_command:
        command: display clock
      register: resultado

    - name: Mostrar resultado
      ansible.builtin.debug:
        var: resultado.stdout
